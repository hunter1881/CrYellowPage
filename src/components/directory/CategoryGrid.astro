---
import TranslatedText from '@components/i18n/TranslatedText.astro'
import type { Category } from '@lib/queries/categories'

interface Props {
  categories: Category[]
  basePath: string
}

const { categories, basePath } = Astro.props
---

<div class="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">
  {
    categories.map((category) => (
      <a
        class="civic-card civic-card-hover flex items-center gap-3 p-4 text-ink no-underline"
        href={`${basePath}/${category.slug}`}
      >
        <span class="flex h-10 w-10 shrink-0 items-center justify-center rounded-civic bg-canopy-soft text-xl text-canopy" aria-hidden="true">
          {category.icon_emoji ?? '#'}
        </span>
        <span class="min-w-0">
          <span class="block text-[15px] font-semibold tracking-[-0.1px]">{category.name}</span>
          <span class="civic-meta mt-1 block"><TranslatedText es="Ver proveedores" en="View providers" /></span>
        </span>
      </a>
    ))
  }
</div>
