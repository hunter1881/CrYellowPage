---
interface Props {
  label: string
}

const { label } = Astro.props
---

<span
  class="inline-flex items-center gap-1 rounded-civic bg-canopy-soft px-2 py-0.5 font-mono text-[10px] font-semibold uppercase tracking-[0.5px] text-canopy"
>
  <span aria-hidden="true">✓</span>
  {label}
</span>
