# Actions

Astro Actions belong here once the project moves from the static MVP to form handling.

When provider registration, reviews, or photo uploads are added, install the Vercel adapter,
switch to server output, prerender SEO pages explicitly, and create `src/actions/index.ts`.
